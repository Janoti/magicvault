# Screenshots da página /features

Coloque aqui os prints (a página os usa automaticamente; se faltar, mostra um
ícone no lugar — então o site nunca quebra).

Nomes esperados (formato landscape, ~1280×720+):
- `collection.png` — tela da Coleção (com valor total, tags, filtros)
- `decks.png`      — um deck com a Análise/Deck Doctor aberto
- `search.png`     — busca por efeito + chips de habilidade
- `trades.png`     — vitrine de Trades / chat
- `profile.png`    — um perfil público (vitrine de decks)
- `premium.png`    — página Premium / beta

Dica: tire os prints no modo escuro, com dados reais bonitos. Depois é só dar
commit das imagens aqui.
